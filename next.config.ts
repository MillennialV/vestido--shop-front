import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
    // Usando modo servidor en desarrollo
    // Para producción, puedes compilar con: npm run build
    // distDir: 'build',      // Carpeta de salida será "build"
}

export default nextConfig